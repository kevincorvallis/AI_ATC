Start a local development server for the frontend.

Run:
```bash
cd /Users/kevin/Downloads/AI_ATC && python3 -m http.server 8000
```

The app will be available at http://localhost:8000

Note: This runs in the foreground. Use Ctrl+C to stop.
