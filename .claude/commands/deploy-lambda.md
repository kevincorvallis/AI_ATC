Deploy the Lambda function to AWS. Run the deployment script and report the result.

Steps:
1. Navigate to the backend directory
2. Run `./deploy.sh`
3. Report success or any errors
4. Show the API endpoint URL if successful
